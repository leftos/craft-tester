repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
        exclude: ^web/tsconfig.*\.json$
      - id: check-added-large-files
        args: [--maxkb=2048]
      - id: check-merge-conflict
      - id: mixed-line-ending
        args: [--fix=lf]

  - repo: local
    hooks:
      - id: web-lint
        name: web oxlint
        entry: pnpm -C web lint
        language: system
        files: ^(web/|data/)
        pass_filenames: false
      - id: web-fmt
        name: web oxfmt --check
        entry: pnpm -C web fmt:check
        language: system
        files: ^web/
        pass_filenames: false
      - id: web-typecheck
        name: web tsc --noEmit
        entry: pnpm -C web typecheck
        language: system
        files: ^(web/|data/)
        pass_filenames: false
      - id: web-test
        name: web vitest
        entry: pnpm -C web test
        language: system
        files: ^(web/|data/|fixtures/)
        pass_filenames: false
      - id: gen-ruff-check
        name: generator ruff check
        entry: uv run --directory generator ruff check .
        language: system
        files: ^generator/
        pass_filenames: false
      - id: gen-ruff-format
        name: generator ruff format --check
        entry: uv run --directory generator ruff format --check .
        language: system
        files: ^generator/
        pass_filenames: false
      - id: gen-ty
        name: generator ty check
        entry: uv run --directory generator ty check
        language: system
        files: ^generator/
        pass_filenames: false
      - id: gen-pytest
        name: generator pytest
        entry: uv run --directory generator pytest -q
        language: system
        files: ^generator/
        pass_filenames: false
