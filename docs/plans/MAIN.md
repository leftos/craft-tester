# Plan index

Entry point for anyone continuing this work. Detailed design, data facts, and rationale: [craft-trainer-v1.md](./craft-trainer-v1.md).

## Current focus

- [x] 1. Scaffold: git, README, CLAUDE.md, ARCHITECTURE skeleton, this index, prek hygiene hooks, dependabot
- [x] 2. Web scaffold: pnpm, Vite + TS (strict flags), oxlint/oxfmt, vitest, exact pins
- [x] 3. zod schema for `AirportData` + `Fixture`, schema export script, sync test, `data/airports.json` (schema needs the SOP YAML's newer fields added in step 9: noise window ids, `exitFixes`/`forcedTransition` conditions, no-DP heading rows, staffing fallbacks, runway-by-turn preference, labelled frequencies, fleet suffixes/airlines, per-runway crossing-restriction flag)
- [x] 4. Generator scaffold: uv project, ruff/ty, `craft-gen --help`, cached HTTP
- [x] 5. CIFP: cycle math, record slicing, SID grouping; 210 KSFO rows as fixture; hypothesis tests
- [x] 6. Charts: API list, PDF download, text extraction; 12 chart-text snapshots (d-TPP text puts a box's value on the line before its label; MOLEN9's transition is named MENDOCINO)
- [x] 7. SOP transcription (`sop.yaml`, `overrides.yaml`, `routes.yaml`) + loader + hash/sentinel verify (`craft-gen verify-sop`)
- [x] 8. Aircraft classes from vNAS (`aircraft_classes.py`, cached `AircraftSpecs.json`, emitted as `aircraftClasses`)
- [x] 9. Merge + emit + integrity checks; first `data/ksfo.json` (findings: 40 gate fixes have no route yet, incl. GOBBS and the SID base fixes; `climbViaEligible` must also count a published top altitude (SNTNA2); schema gained `airport.lat/lon`, `sids[].baseFix`, `scenario.activeNotices` for the engine — generator must emit the first two)
- [x] 10. Rules engine core + table tests + exhaustive enumeration test (5,304 of 5,376 combinations resolve; the 72 left are the non-DP runway-heading noise row, which v1 does not clear — the generator must avoid P non-RNAV off 01 at night). Validation questions raised: the late-night "NIITE# GOBBS" south row is dead data (NIITE4 has no south transition; the engine would have to amend the route, an amendment-mode concept); MOLEN9 is reachable only when MOLEN is filed as the exit fix since ENI is a north gate
- [x] 11. Synthetic fixtures + fixture runner (27 fixtures in `fixtures/ksfo/synthetic/`, 25 settled; `fixtures.test.ts` prints what the engine makes of every pending plan: 66 of 70 worksheet plans resolve)
- [x] 12. `options`, `grade`, `speak` (full-route reading repeats the vector fix for radar-vector routes; the expect clause runs straight into the altitude, "flight level three five zero one zero minutes": polish in step 14)
- [x] 13. Scenario generator (seeded): `generateScenario` / `drawScenario`; 7 redraws in 1,000 seeds, all the non-RNAV-prop noise row
  - [x] Follow-up (landed 2026-09-15): `CONFIG_WEIGHTS` in `generate.ts` named KSFO config ids; the training weight is now a required `runway_configs[].training_weight` in `sop.yaml`, emitted as `runwayConfigs[].trainingWeight`, so no code is keyed on KSFO. Same values, same order, so seeds keep drawing the same scenarios
- [ ] 14. UI: strip, ATIS, CRAFT form, results, seed in URL hash (dispatched 2026-09-15 as one brief, may return partial). The parallel data brief landed in `52137a9`: EHF gate, `R-AIRWAY`/`R-NAVAID` rows, `propose.ts` using `routeLabel`, `import-worksheets --check` in CI
  - [x] Playtest 2026-09-15 (Claude in Chrome against `pnpm -C web preview`, UI merged in `addd516`): seeds 1 (TRUKN2 jet, 28 RT), 5 (C172/A stale GAPP6, radar vectors Oakland VOR, "Victor one zero seven Cathe, Victor one eleven Salinas VOR, direct"), a (GL5T SFO5 SAC off 01R, climb via SID except maintain 10,000) all grade 6 of 6 with citations; a wrong run on seed 2 grades 1 of 6 with red rows; procedure change clears the fix; hash restores the scenario after reload and the share link matches; phone width is a single column with no horizontal scroll; no console errors. A dark page was the user's Dark Reader extension, not the theme
  - [x] Playtest findings to fix (landed 2026-09-15): picking "no SID" reads back as "you said: no SID"; **user steer 2026-09-15**: each result row shows "you said: …" and a checkmark when it matches, and a "correction: …" line only when it differs (replacing the always-shown "clearance: …" line)
  - [x] **User steer 2026-09-15** (landed `027b86c`, browser-checked): revisiting an already solved scenario (a seed the student submitted before, remembered per browser in localStorage) shows the solution collapsed behind a click-to-expand spoiler instead of the open results, and offers a "Retry" button that opens a blank form without showing the past answer. Also offer "Retry" on the fresh results view. Design: `solved:<icao>` map seed → submitted picks in localStorage (try/catch, per-viewer convenience only); `AppState.view` gains a `revisit` flag; results panel renders inside `<details>` when revisiting (dispatched 2026-09-15, web worktree; runway-element step 1 runs in parallel in the gen worktree)
  - [x] **User steer 2026-09-15** (landed through `9a56f5a`, browser-checked on seed `f`): the ATIS advertises the configuration's departure runways ("departing 01L, 01R" in 28/01), never the exact runway; the student picks the runway in the "expect runway" element after F and is graded on it, citing the configuration row and one of the `RWY-*` rows; an on-request draw files `REQ RWY 28` on the strip — see [archive/runway-element.md](./archive/runway-element.md). User decision: the transmission ends "Expect runway two eight left" (landed `e19996c`)
  - [x] **User steer 2026-09-15**: scenario filters in the header: time of day (day / night / either) and runway configuration (a specific config, any SFOW, any SFOE, any). The filter constrains the draw (config picked among the allowed ids by their weights; time drawn inside or outside the noise windows) and rides in the URL hash next to the seed so a shared link reproduces the filtered scenario; remembered per browser as a convenience (landed 2026-09-15, browser-checked: Night + Any SFOE drew 19/19 at 0119L, share link `#s=xjr8h&t=night&c=plan:SFOE` survives reload; day/night seeds differ from "either" seeds by design; switching airport keeps the time filter in memory but does not store it until the dropdown is touched)
  - [x] **User steer 2026-09-15**: drop the C (clearance limit) dropdown and verdict from the page; the destination is trivial to test and, like the squawk, is only spoken in the transmission. The engine keeps resolving `clearedTo` for the reveal and the fixtures; `PlayerPicks`, `grade`, `options`, the form and the results lose the element (six graded: R.sid, R.route, A.phrase, A.expect, F, RWY) (landed 2026-09-15)
  - [x] **User steer 2026-09-15** (landed 2026-09-15): the SID is part of the flight plan, and training defines a *clean clearance* as one read aloud exactly as filed. Clearance mode must therefore draw only clean plans: the filed route carries the SID the SOP assigns, current version, and no SID-less or wrong-SID draws (`pickSidToken` in `generate.ts` drew correct / none / wrong on purpose; that becomes always-correct). A plan whose SID is missing, stale or wrong for the configuration is amendment-mode material; the engine still resolves the clearance for such a plan (that is what the amendment route check needs), so the dirty synthetic fixtures stay as engine fixtures. **User decisions 2026-09-15**: (a) draw only clean plans and **drop R.sid from grading** (like the clearance limit: resolved, spoken in the reveal, not a dropdown; five graded elements remain: R.route, A.phrase, A.expect, F, RWY); (b) amendment mode is *amend, then read the clearance for the amended plan*: step 19's engine also returns the corrected scenario and step 20 chains the amendment form into the CRAFT form (queued as a web brief after steps 19.1–19.2 land)
  - [x] **User steer 2026-09-15** (landed 2026-09-15): in the route → shape dropdown the fourth shape must not read "then as filed"; it is the bare fix with no prefix, for a filed exit fix of the SID that is not one of its transitions. "Then as filed" always follows the SID and the transition or exit fix, whatever the shape (ZOA CRAFT Phraseology reference). So the shape label becomes "(no prefix)" and the results label the bare fix (`TRUKN`), not `TRUKN, then as filed`; the `as_filed` template id and the `R-AS-FILED` row stay
  - [x] **User steer 2026-09-15** (landed 2026-09-15): in clean (clearance) mode the CRAFT form lists C (the clearance limit), the SID half of R, and T (the squawk) as filled, non-editable rows, so the form flows like a CRAFT; they are neither picked nor graded, just shown in place
  - [x] **User steer 2026-09-15** (landed `5f2a3be`; the 22 fixture notes that still narrated the DAL653 reading were rewritten in `84aed9a`): when a SID publishes a top altitude (e.g. NIITE#) and the filed altitude is at or above it, the altitude is plain "climb via SID", never "climb via SID except maintain (chart top altitude)". **User decision 2026-09-15, reversing DAL653, on the SOP's own words** ("SFO ATCT must issue interim altitudes as specified below when a Top of Climb (TOC) or Top Altitude is not published", SFO SOP 2-2 c (1); the altitude rows' `source` cite it and their `text` quote it): every published-top SID (TRUKN2, NIITE4, SSTIK5, SAHEY4, CIITY3 at FL190; SNTNA2, GNNRR3 at 3,000) reads plain "climb via SID" when the filed altitude is at or above the chart top; the SOP 2-2 c interim rows apply only to SIDs without a published top (GAPP7, SFO5, MOLEN9, SEGUL1, WESLA5). Filed below the top keeps "climb via SID except maintain (filed)". Data toggle: all five `altitude_rules[].when_top_altitude_published` become `climb_via`; the settled fixtures that change are re-settled to the engine's new output (dispatched 2026-09-15, web worktree). Example from the user: seed `etebh9` in 28/01, JBU223 A20N/L to KRNO at 21,000 on `NIITE4 ORRCA TARVR SPOON` at 0049L; the engine said "climb via SID except maintain 10,000" citing A-CLIMB-VIA-EXCEPT, whose text ("…but no top altitude is published…") even contradicts the chart; expected: "climb via SID". Add it as a synthetic fixture
  - [x] **User steer 2026-09-16 (ZOA senior staff, relayed by the user)** (landed 2026-09-16, browser-checked on seed `1`: the 10-minute pick shows an amber row, "shorter: no expect altitude", citing `A-EXPECT-REDUNDANT`, score "4 of 5 elements correct, 1 acceptable but inefficient"; the 3-minute pick stays red; phone width clean): speaking "expect filed altitude 10 minutes after departure" when the chart already publishes the note is not wrong, just inefficient, so the A-expect element should grade as a warning (orange) rather than a miss (red) in that case. Needs a third verdict state in `grade.ts` (correct / acceptable-but-inefficient / wrong) with its own colour in `results.ts`, a rule row that says which redundant readings are acceptable (start with the expect clause the chart already carries; the user decides whether "then as filed" after a bare fix or a spelled-out full route join it), and the score line deciding whether a warning counts toward "N of 5". Plan the states before touching the grader. **User decisions 2026-09-16**: an acceptable verdict counts as correct, the score line reads "5 of 5 elements correct, 1 acceptable but inefficient"; acceptable only when the clause was dropped because the chart carries it and the student picked the chart's delay (10 minutes), 3 minutes there stays wrong, and a clause spoken when the flight is cleared to the altitude it filed stays wrong. Design: `ResolvedClearance.redundantExpect` (the clause the chart already speaks, set only on the chart drop), `Grade.verdict: correct | acceptable | wrong` replacing `ok`, `phraseology_rules` row `A-EXPECT-REDUNDANT`, no new toggle; fixture shape unchanged. "Then as filed" after a bare fix and a spelled-out full route cannot join in v1, since the route element is graded on shape + fix and those tails are only spoken: noted on the v2 free-text item
  - [ ] Playtest observations for the user: the standing `SFO-SEGUL-OFF` notice is in force on 80% of scenarios (`NOTICES_OFF_CHANCE`); seed `f` puts a heavy UPS A306 filing `TRUKN CCR CCR2` (no 28-only SID) on 28L in 28/01 with the remark `REQ RWY 28` (the generator's on-request draw does not require a 28-only filed SID the way the importer does), and the engine then issues GAPP7 radar vectors TRUKN, maintain 3,000, because TRUKN is no SNTNA2 transition and TRUKN# is off the 28s only in 28 RT (a trainer might amend to SNTNA2 instead: amendment mode); the extension's ref-based click on the submit button failed once where a coordinate click worked (tool quirk, not the app)
- [ ] 15. CI + GitHub Pages (workflows landed, pinned SHAs, actionlint + zizmor clean); finalize README, ARCHITECTURE, CLAUDE.md at the end
- [x] 16. `import-worksheets`: seven Google Docs → 70 pending fixtures (18 phraseology + 52 amendment) in `fixtures/ksfo/worksheets/`. Departure runway defaults to the config's first runway pending validation. Findings for the validation loop: UAL313 files `/Q` (not in FAA table 5-4-1, so treated as non-RNAV; likely a deliberate wrong box) and `BVLQ124` (sheet typo); four rows are truncated in the source (KAL65 ×2, NAX7068, VOI5909)
- [x] 17. Validation loop, clearance mode: **all 18 phraseology plans settled 2026-09-15** (Practice 1A, 2, 3), one at a time with the user; every correction landed as data or as a phraseology rule. Record of the questions and decisions:
  - [x] A. User decision 2026-09-15, SOP 2-2 a wins over the CBT: northbound GAPP# is runway 28 only, SFO# is runway 01 only, and every northbound row is P/T/J. Drop the CBT row `SFOW-N-GAPP-PROPS`; widen TRUKN/SNTNA rows to P/T/J (still RNAV-gated). Props and turboprops off the 01s filed GAPP7 OAK/SGD get SFO# radar vectors (the TEC table agrees: `SFO# OAK V6 SAC` off 01, `GAPP# OAK V6 SAC` off 28) (dispatched)
  - [x] B. User decision 2026-09-15: SOP wins, GAPP# off the 28s. SNTNA# is not an option for "SFO4 RBL" because RBL is not a SNTNA2 transition; issuing it would need a route amendment, which is amendment mode. The Phraseology 3 sheet ("routes correct") files SFO4 for this heavy; raise with the trainer if the loop shows the sheet meant SFO# (dispatched)
  - [x] C. User decision 2026-09-15: drop the expect clause when the filed altitude equals the assigned one: `phraseology.expect_altitude: only_when_interim_below_filed`. The engine's first reading of that mode dropped the clause on every plain "climb via SID" (no interim to compare); corrected so a plain climb-via compares the filed altitude with the SID's published top altitude (TRUKN2 FL190, filed FL320 → clause kept) (landed in `c89f636`; the mode was then replaced by `unless_chart_publishes_it`, next item)
  - [x] User rule 2026-09-15: procedures and fixes are never spelled letter by letter in a clearance (only at pilot request); a SID is always "(name) departure", a STAR always "(name) arrival", navaids by name. The letter-by-letter fallback in `speak.ts` must become unreachable for checked data: the generator reads navaid names from the CIFP `D` (VHF) and `DB` (NDB) records for every navaid token in the route library, TEC routes, gates, SID transitions and imported worksheet routes, emits them in `fixSpoken` (hand `fix_spoken` in `overrides.yaml` wins), and fails the build on a navaid with no name (dispatched)
  - [x] User decisions 2026-09-15: "Oakland Six departure, radar vectors Sacramento VOR, then as filed". Every navaid, everywhere in the clearance, is spoken as name plus type; VOR, VOR/DME and VORTAC are all "VOR", NDB "NDB", TACAN "TACAN"; transitions keep their published chart name. Encoded as the emitted `fixSpoken` value itself ("Sacramento VOR"), derived from the CIFP `D`/`DB` record class; hand `fix_spoken` rows become corrections only and are emitted verbatim (dispatched)
  - [x] User rule 2026-09-15, full-route grammar (landed): after the SID phrase, each fix reached without an airway is "direct (fix)"; a fix reached via an airway is "(airway) (fix)"; a trailing STAR is "(name) arrival"; a route that ends on a fix with no STAR ends with a bare "direct". Examples: `OAK6 SAC DEDHD FILMR2` → "Oakland Six departure, radar vectors Sacramento VOR, direct Dedhd, Filmr Two arrival"; `OAK6 SAC DEDHD` to KSMF → "…, radar vectors Sacramento VOR, direct Dedhd, direct" (dispatched; airway form to confirm on N483KA)
  - [x] User rule 2026-09-15 (N172SP, `GAPP7 EUGEN`): when nothing follows the exit fix, the abbreviated clearance ends "radar vectors Eugen, direct" (or "Ntell transition, direct"), never "then as filed" (landed)
  - [x] Navaid names, facility words, transition split and the full-route grammar landed (53 navaid names from CIFP 2609; SWR is now "Palisades VOR"). Left open: `ECA`, `SMA`, `KAE` appear only on worksheet-filed routes and have no CIFP record (Manteca decommissioned; Kake is not in the FAA file), so the build warns and the speaker spells them; add hand `fix_spoken` rows if a trainer wants them read by name. `data/ksfo.json` now depends on `fixtures/ksfo/**` routes: re-run `craft-gen build` after an import
  - [x] Navaid-name findings (agent, 2026-09-15): Alaska navaids sit under customer area CAN in the CIFP, so the parser reads the whole file and prefers K1–K7 on a collision; 31 US idents are ambiguous NDB pairs (none used by KSFO) and are omitted, VHF preferred over NDB on identical names; `ECA` (Manteca, decommissioned) and `SMA` are in no CIFP record, so `routes.yaml` KLVK tail becomes `OAK V244 ALTAM` and the RKSI tail ends at MDO; `KAE` on a worksheet route is unnamed and only warns; `MC COOK` gets a hand row "McCook VOR"; SWR is now named Palisades in CIFP 2609. Transitions keep the bare navaid name (`transitions[].spoken`), `fixSpoken` carries the type word, and `speak.ts` must read the transition name from the SID, not from `fixSpoken`
  - [x] User rule 2026-09-15 (FDX1563, UPS2896; SOP 2-1 e): oceanic, Far East and cargo flights usually need 28L/R for performance; when 01 is the advertised departure runway they may be issued 28L/R and the matching SID. A cargo or heavy flight that files a 28-only SID (WESLA5, SNTNA2) in 28/01 is treated as requesting the 28s, and the request is honoured: FDX1563 → 28L, WESLA5 NTELL, climb via SID except maintain 3,000, 135.1; UPS2896 → 28L, SNTNA2 ORRCA, 10,000, 120.9. Model: `departure_runways[].on_request_for: [cargo, heavy, oceanic]` on the 28L/R rows of 28/01, `routes.yaml` `cargo_airlines`, heavy from the vNAS wake category; importer and scenario generator pick that runway when the flight qualifies and (importer) the filed SID is published only for that family; runway choice order becomes class default → on-request → direction preference (landed in `cf1fdd1`; jets that cannot request now never draw the 28s in 28/01)
  - [x] Full-route grammar confirmed by the user on CMD70, N172SP, N436MS; N903JP validated (SSTIK5 YYUNG under the SEGUL notice, full route "direct Gaviota VOR, direct Habut, direct")
  - [x] Bug (Practice 3, FFT425): the speaker read `Q174` as "Q one seventy-four". User rule 2026-09-15: T and V are read phonetically ("Tango", "Victor"), J and Q as the alphabet letter ("Jay", "Queue"); other airway letters phonetic; number in group form (landed)
  - [x] `R-AIRWAY` (7110.65 2-5-1, group form) and `R-NAVAID` (2-5-2, facility word) phraseology rows landed in `52137a9`; the reveal's airway and navaid readings are now cited
  - [x] Validated 16/18 phraseology plans: all six Practice 3 plans (ACA452, SWA1699 SNTNA2 10,000 120.9; FFT425, SWA2096, SWA436 WESLA5 3,000 135.1, SWA436 under the SEGUL notice; FDX1563 heavy GAPP7 radar vectors Red Bluff VOR, maintain 3,000) confirmed 2026-09-15 with full-route readings; FDX1563 and UPS2896 on Practice 2 await the on-request 28s (dispatched)
  - [x] Validated 10/18 phraseology plans (N903JP added): + N483KA, CMD70, N436MS (GAPP7 off 28R, radar vectors fix, maintain 3,000), N172SP (same, 135.1; spoken form pending the "direct" rule)
  - [x] User source 2026-09-15: ZOA "CRAFT Phraseology" reference (Google Doc `1Jq0uqpuhIZRSfKvYbyJvAplT1h6zf-wMhcAN6MAQC6k`, retrieved 2026-09-15). Add it to `sop.yaml` `secondary_sources` and cite it from the `phraseology_rules` rows. Rules it adds or confirms:
    - [x] confirms: "(fix) transition"; "radar vectors (fix)"; "then as filed" only when the last spoken fix or airway is already on the flight plan; full route: two fixes without an airway "(fix) direct (fix)", with an airway "(fix) (airway) (fix)"; arrival "(fix) (name) arrival"; no arrival "(fix) direct"; "climb via SID" / "climb via SID except maintain" / "maintain"; nothing for the final altitude when the aircraft has a SID and the altitude was not amended; "departure frequency (freq)"; "squawk (code)"
    - [x] new: **"radar vectors to join (airway)"** when the first filed element after the SID is an airway; landed as route template `radar_vectors_airway` (only vector SIDs are compatible; the gate direction comes from the first fix after the airway). `propose.ts` now prints the engine's `routeLabel` ("radar vectors to join V6", "TRUKN, then as filed")
    - [x] new: **pilot-nav SID with a filed fix that is not a transition** is spoken as the bare fix (landed; grading an as-filed answer now also checks the fix). User decision 2026-09-15 for the base-fix case `TRUKN2 TRUKN CCR CCR2`: abbreviated "Trukn Two departure, Trukn, then as filed"; full route "Trukn Two departure, Trukn, direct Concord VOR, Concord Two arrival". So the `as_filed` route template carries the exit fix and the speaker names it; `R-AS-FILED` text and the as-filed fixtures gain the fix
    - [x] airways: user confirmed group form, "Victor two forty-four"
    - [ ] new: heading departures are "(via) turn left/right heading (xxx)" ("via turn right heading 315"), for the non-DP heading rows (v1 does not clear them; needed when they are)
    - [x] answers the amendment-mode question: the amended form keeps the tail: "expect amended flight level two seven zero one zero minutes after departure"
    - [x] airways are spoken "Victor 6", "Tango 8", "Jay 5", "Queue 2"; three-digit airways in group form ("Victor two forty-four"), user 2026-09-15
  - [x] `speak.ts` full-route reading spells a filed SID token letter by letter and calls it an arrival ("golf alpha papa papa Seven arrival" for GAPP7); strip the filed SID token from the full-route reading as `parseFiledRoute` does, and read a real STAR token as "(name) arrival" only when it ends the route
  - [x] User rule 2026-09-15 (first example, UAL320 TRUKN2): the expect clause is not spoken when the final altitude is as filed and the SID chart already says "expect filed altitude N minutes after departure", unless the facility SOP overrides (SFO: non-standard interim → "expect filed altitude three minutes after departure"). All 12 KSFO charts carry the note. Encoded as `sids[].chartExpectFiledAltitudeMinutes` parsed from the chart text and `phraseology.expect_altitude: unless_chart_publishes_it`, replacing `only_when_interim_below_filed` (the filed-equals-assigned drop stays as a sub-condition). Landed: all 12 charts parse to 10 minutes; 25 synthetic fixtures lost their expect clause; `ws-phraseology-practice-1a-ual320` is the first **settled** worksheet fixture
  - [x] ~~User decision 2026-09-15 (DAL653): the SOP 2-2 c interim table applies even when the SID publishes a top altitude, so TRUKN2 jets get "climb via SID except maintain 10,000"; all five altitude rows became `when_top_altitude_published: interim`~~ **Superseded the same day** by the SOP's own sentence (2-2 c (1): interim altitudes are issued "when a Top of Climb (TOC) or Top Altitude is not published"): the five rows go back to `climb_via`, so published-top SIDs read plain "climb via SID" when the filed altitude is at or above the top; see the step 14 entry with the `etebh9` example. DAL653 and the other affected settled fixtures are re-settled to the engine's output
  - [x] User steer 2026-09-15 (N483KA): in 28/01, GA aircraft up to light jets depart 28R at intersection E. Landed as `default_for_classes: [P, T]` on the 28R row of the 28/01 config (`9398209`); 11 worksheet plans moved to 28R. User chose P and T only for now; a GA-jet flag on the fleet is a later data addition if a plan needs it
  - [x] `import-worksheets` now keeps settled fixtures and refuses to overwrite one whose scenario changed (`--overwrite-settled` to downgrade). CI runs `import-worksheets --check` (network-enabled: Google Docs + vNAS) so a stale committed fixture fails the build
  - [x] Validated 5/18 phraseology plans: UAL320, DAL653 (TRUKN2, transition, climb via SID except maintain 10,000, 120.9), ASA325, NKS188 (SSTIK5 off 01L, 10,000, 135.1), SWA333 (SEGUL1 filed, SSTIK5 YYUNG issued under the SEGUL-off notice). N483KA re-proposed after the 28R change
  - [ ] Filed altitude below a SID's published top altitude (TBM9 filed 11,000 on TRUKN2, top FL190): a plain "climb via SID" would climb it past what it filed, so the engine now issues "climb via SID except maintain (filed)" per 7110.65 4-3-2 c 4, with no expect clause (landed in `c89f636`; confirm with the user in the loop)
  - [ ] Route library vs TEC (found by the step 20 fault injector, 2026-09-15): every NCT destination's library tail differs from the transcribed TEC route (KSMF files `TRUKN CCR CCR2`, TEC-KSMF-SFOW-J says `TRUKN# TRUKN FEVTA FEVTA1`) and some library altitudes exceed the TEC cap (KSMF 11,000 vs 10,000), so those nine plans read as clean in clearance mode but always take a route amendment in amendment mode. **User decided 2026-09-15: the library carries the TEC route and its cap.** A TEC route is what ATC must issue for those city pairs, so this is not only an amendment-mode inconsistency — clearance mode grading `SGD` to KAPC as correct-as-filed is wrong too. The library becomes the correctly-filed baseline and wrong plans come only from the step 20 fault injector, which is deliberate rather than accidental. Surveyed 2026-09-15, the nine rows are not all wrong: **KSMF `TRUKN FEVTA FEVTA1` @9/10k and KSMF `OAK V6 SAC` @5/6k already agree**; **KMRY `EUGEN` @7k** has the right route but exceeds the 5,000 cap of `TEC-KMRY-SFOW-TP` (the row is `classes: [P]`); the rest differ in route — **KLVK** `OAK V244 ALTAM` @5/7k vs `GAPP# OAK V244 ALTAM MOD` cap 5,000 (missing `MOD`), **KWVI** `OAK V107 CATHE V111 SNS` @7k vs `GAPP# OSI V25 SNS` cap 5,000, **KOVE** `OAK V6 SAC V23 YUBBA` @17/19k vs `GAPP# OAK V6 SAC` cap 5,000 (12,000 over), **KAPC** `SGD` @4k vs `GAPP# SAU` cap 5,000. Note a TEC row is keyed per plan, so covering both plans at one destination means two library rows, not one edit: KSMF has `TEC-KSMF-SFOW-J` (`TRUKN# TRUKN FEVTA FEVTA1`) and `TEC-KSMF-SFOE-J` (`CIITY# CIITY FEVTA FEVTA1`). **Scope split 2026-09-15**: the two bogus KSMF rows go with the arrivals work below, because the CIFP check fails the build on them; KLVK, KWVI, KOVE, KAPC and the KMRY cap stay here as a focused pass, since those are route-design changes rather than data errors. **Landed 2026-09-16** — see [route-library-tec.md](./route-library-tec.md): not a data pass after all (the clean tail depends on the configuration, class and suffix; the TEC lookup ignored what the SOP assigns). User decisions 2026-09-16: the draw verifies every plan with the amendment engine and redraws a dirty one, so the library is the correctly-filed baseline by construction; a TEC row applies only when the SOP would issue the DP it begins on to this flight here (the engine clears the candidate TEC route to find out); NCT rows become the TEC tails, 34 → 43 rows. `library.test.ts` proves every row and altitude clean somewhere; it corrected KDEN and PHNL parity and removed the KPDX `DEDHD LMT OCITY#` row, which the Portland LOA never allowed (it was the SWA2021 amendment plan). Left for the user: a browser check of a KLVK and KSMF draw in both modes. **User rule 2026-09-16**: radar-vector SIDs can be vectored to any initial fix, unlike pilot-nav and hybrid SIDs (already how `servesExitElement` reads)
  - [x] CYVR files a Seattle STAR (user 2026-09-15, seed `oml270`): `routes.yaml:121` was `{ exit_fix: DEDHD, destination: CYVR, tail: DEDHD LMT BTG HAWKZ7 }`, but HAWKZ# is the KSEA RNAV arrival — the repo's own LOA row `LOA-ZSE-SEA-ROUTE` says "SEA arrivals from the Bay: ..RBL..LMT then the HAWKZ STAR". The row is a faithful generalisation of worksheet phraseology-practice-3 (ACA452 KSFO→CYVR files `SNTNA2 DEDHD LMT BTG HAWKZ7`), so the sheet itself is wrong. Nothing in `merge.py` checks that a route's trailing STAR belongs to its destination — it only checks the destination is in `destinations`. **User decided 2026-09-15**: give the row a real Vancouver arrival and warn on build. The tail becomes `DEDHD LMT BTG J1 SEA PAE GRIZZ1` — the CZVR preferred-routing table (https://czvr.ca/preferredrouting) publishes `KPDX - BTG J1 SEA PAE GRIZZ# (EVEN ALT)`, and GRIZZ is at revision 1 after the 27 Nov 2025 Nav Canada CYVR STAR redesign (AIC 022/2025). CYVR is outside the FAA CIFP, so that revision number is hand-maintained. The synthetic fixture `syn-sntna2-dedhd-28l-2801` carries the same tail and moves with it; the worksheet fixture `phraseology-practice-3-aca452` stays verbatim, because the sheet is what ZOA published. The build warning is two signals, since "no LOA/TEC row ties the arrival to the destination" would fire on nearly every row: a trailing procedure family an LOA `kind: route` rule names for a *different* destination set, and a trailing family that two or more destinations both file. **Landed**: `_warn_route_arrivals` in `merge.py` (both signals fired on the bad row before the data fix, neither fires now), `test_a_route_tail_ending_on_another_destinations_arrival_warns` in `test_merge.py`, data rebuilt. Known residue: the worksheet fixture still files `HAWKZ7` to CYVR and the check cannot see it, because fixture routes feed only `fixSpoken` and carry no destination; surfacing it would mean running the check over `inputs.fixture_routes`. `_PROCEDURE_TOKEN` also reads a 3-letter navaid followed by a digit (`RBA4` on the KMCI tail) as a family — harmless today, no LOA row names `RBA` and only KMCI files it
  - [x] **Trailing arrivals are unchecked and mostly wrong (user steer 2026-09-15, correcting the "serves one airport" heuristic above)**: a STAR can serve several airports — what makes it right is being *published for that airport*, which the CIFP states directly. STARs are record type `E` (`line[12]`), same fixed-width slicing `records.py` already uses for SIDs (`D`) and runways (`G`); the nationwide `FAACIFP18` carries them for every US airport. Checking cycle 2609 against the 19 trailing tokens in `routes.yaml`: **4 correct** (FEVTA1/KSMF, IRNMN2/KLAX, COMIX2/KSAN, MAGGI3/PHNL); **10 stale revisions** (HAWKZ7→HAWKZ8, OCITY7→OCITY3, TMBRS2→TMBRS4, MADEE4→MADEE7, LONGZ1→LONGZ4, SADYL3→SADYL4, SSKII1→SSKII4, COKTL3→COKTL4, ROKKR2→ROKKR3, LEGOZ1→LEGOZ4); **4 families the destination does not publish at all**, on 6 rows — `WAATS5`/KSLC (×2 rows; KSLC publishes BEARR5 BVL2 CARTR1 JAMMN5 JAZZZ1 LHO5 PITTT2 QWENN7 SKEES6 SPANE8 YUTES2, and the tail already files BVL, so `BVL2` is the likely intent), `RBA4`/KMCI (BQS8 JHAWK8 JSONN4 MHOMS3 RUDDH3 TYGER7 WUTNG3), `CCR2`/KSMF (×2 rows; FEVTA1 SLMMR5 SUUTR4 TUDOR2 — this is the same row plan item 66 already flags against TEC-KSMF-SFOW-J), `SUNST4`/KLAS (BLAID2 CHOWW4 COKTL4 CRESO5 GAMES2 ISHEE2 JAYSN3 LARKK1 PUMLE1 RKSTR4 RNDRZ4); **1 unverifiable** (GRIZZ1/CYVR — zero CIFP rows for CYVR, so only the LOA signal can ever see that one). **User decided 2026-09-15**: routes.yaml files the family form (`HAWKZ#`, the `SID_PLACEHOLDER = "#"` convention `tec.yaml` already uses for DPs) and the build resolves the current revision from the CIFP, so the 10 stale ones self-heal every cycle; a family the destination does not publish **fails** the build; a destination with zero CIFP rows (CYVR and the other foreign ones) keeps a literal hand-maintained token and is skipped by the check. Proposals for the 6 bogus rows, all from the cycle-2609 CIFP rather than the web — **KSLC ×2** `BVL WAATS5` → `BVL BVL2` (BVL2 has no enroute transition and its legs run BVL → KNOLE → **WAATS**, so `WAATS5` was a fix on the chart misread as the procedure name); **KLAS** `BTY SUNST4` → `BTY PUMLE1` (PUMLE1 publishes a BTY transition beginning at BTY); **KMCI** `MCK PWE RBA4` → `MCK DPEAK JSONN4` (JSONN4's DPEAK transition begins at DPEAK, N40 05 W096 33, ~20nm west of the PWE the tail files, so DPEAK is reached first); **KSMF ×2 — both rows deleted** (user 2026-09-15: "CCR CCR2 has been replaced by FEVTA FEVTA1… the replacement is FEVTA FEVTA1, no CCR"). The CCR2 arrival was superseded by FEVTA1, which begins at FEVTA rather than CCR, so the replacement drops CCR entirely: `TRUKN CCR CCR2` becomes `TRUKN FEVTA FEVTA1`, which is already the row directly below it, and `CCR CCR2` becomes `FEVTA FEVTA1`, which cannot be a row because FEVTA is in no gate and is no SID's transition, so `exit_fix: FEVTA` fails `_check_gate_fixes`. Both collapse into the surviving `TRUKN FEVTA FEVTA#`; KSMF keeps that for T/J and `OAK V6 SAC` for props. Cost: CCR is no longer any route's exit fix, so the unused-gate-fix warning goes 41 → 42. **Superseded idea, not built**: an earlier pass here proposed turning `TRUKN CCR CCR2` into the SFOE variant `{ exit_fix: CIITY, tail: CIITY FEVTA FEVTA# }` purely to avoid the duplicate. Its rationale is gone, but the gap it noticed is real — `TEC-KSMF-SFOE-J` (`CIITY# CIITY FEVTA FEVTA1`) has no library row, and CIITY is in `gates.north` with CIITY3's `baseFix` at CIITY published for the east-plan runways 10L/10R/19L/19R, so the row would be valid. Left as a candidate for item 66's remaining pass rather than smuggled into a data fix. `SUNST`, `RBA` and `CCR` appear as a fix on no current STAR at their destinations, so unlike WAATS they look like retired procedures the ZOA route tool still listed. **Landed 2026-09-15** in three briefs. **A**: `cifp/stars.py` with `parse_star_ids(lines) -> dict[str, frozenset[str]]`, a 179-row checked-in fixture covering all 42 library destinations (the first cut used only the 31 with route rows, which left KBFI — the very airport that proves an arrival can serve two — out of its own test), 9 tests, and a `destination_stars` field on `BuildInputs`; no output change. **B**: `_PROCEDURE_FAMILY_TOKEN`, `_resolved_arrival` and `_resolved_tail` in `merge.py`, called from `_route_entry`; the three-way rule keyed on `airport_records`, which distinguishes a foreign destination (nothing to resolve, literal required) from a US airport publishing no arrival (a trailing procedure token is provably wrong) — O88 belongs to the second group, not the first, and blank padding (`"O88 "`) hides that from a naive grep. A literal revision at a destination the FAA file carries now also fails, which is what stops stale revisions returning. 6 new tests, routes 36 → 34. **C**: 10 synthetic fixtures moved to current arrivals, `speak.ts` doc comments re-worked from the retired `CCR2`/"Concord Two" to `BVL2`/"Bonneville Two". Web unit-test literals and worksheet fixtures were deliberately left on the old tokens — the first are pure logic inputs, the second are transcriptions CI checks against the published sheets. **Non-finding, recorded so it is not "fixed" again**: the single-digit `STAR_TOKEN` in `speak.ts` and `SID_TOKEN` in `route.ts` (`/^[A-Z]{3,5}\d$/`) look narrower than the generator's `_PROCEDURE_TOKEN` (`\d+`), but they are correct. No SID or STAR in the FAA CIFP has a two-digit revision — across 1,075 SIDs and 683 STARs in 2609 the revisions run 1-9, and the ARINC 424 procedure field is six characters, so a five-letter name physically cannot carry two digits. The only multi-digit ids in the file (`1U71`, `L711`, `O431`, `U142`) are numeric-style airport identifiers that match neither pattern. The generator's `\d+` is looser than reality but harmless
  - [ ] SFO5 off the 01s: the override says the 01 side has a crossing restriction (SFO 6 DME at or above 3,000), so the engine says "climb via SID except maintain"; the 28 side has none, so "maintain". Confirm with the user on the first worksheet example
  - [x] Worksheet import picks the departure runway from `direction_runway_preference` by the filed exit fix's gate (23 northbound 28/01 plans moved 01L → 01R; UAL320 now resolves to TRUKN2 DEDHD transition, climb via SID)
  - [x] `worksheets.yaml` `type_aliases: {A32N: A20N}` applied on import (2 plans)
  - [x] EHF (Bakersfield) added to `gates.south` (`52137a9`): both LXJ351 amendment plans resolve, so all 70 worksheet plans resolve (52 pending amendment plans, 18 settled). The no-gate runway fallback test now uses PYE, which is in no gate
  - [ ] **Stop hand-rolling CIFP parsing (user steer 2026-09-15: "you shouldn't be re-implementing your own cifp parser when both zoa-reference-cli and x:/dev/yaat have implementations")**. `generator/src/craft_generator/cifp/` now holds five hand-rolled ARINC 424 parsers — `records.py` (SID/runway), `airports.py`, `navaids.py`, `sid.py` and the new `stars.py`. Four predate this work; `stars.py` was added in the arrivals pass above without checking for an existing implementation, and it duplicates `parse_star_record()` and `get_all_stars(airport)` that `zoa-reference-cli` already exposes in `src/zoa_ref/cifp.py` (2,072 lines, 13 CIFP test modules, `https://github.com/leftos/zoa-reference-cli.git`, so a git dependency would resolve in CI). Three reuse targets, in increasing order of correctness: (1) depend on `zoa-reference-cli`, which has the exact two functions but is a CLI application rather than a library; (2) depend on **`cifparse` 2.0.9**, the upstream spec-checked package both other projects defer to — `x:/dev/yaat/.claude/rules/cifp-parser.md` says outright "if a column offset in YAAT's parser disagrees with `cifparse`, **trust cifparse**", and records a systematic off-by-one YAAT had in procedure leg fields; (3) keep the hand-rolled parsers but validate every column offset against `cifparse`. Note `zoa-reference-cli/docs/plans/cifp-parser-drift-from-yaat.md` already tracks drift between two implementations and found field-level bugs including an altitude off-by-one and a ×10 scaling error (all 9 fixed 2026-05-21) — a third implementation in this repo makes that problem worse, which is the real cost, not the duplicated lines. **User decided 2026-09-15: replace all five with `cifparse`**, and the arrivals work was committed first rather than blocked on it, since its behaviour is right whichever parser backs it and `stars.py` hides behind a two-function API. Scope for the task: add `cifparse` as a generator dependency, port `records.py`, `airports.py`, `navaids.py`, `sid.py` and `stars.py` onto it, delete them, and keep the checked-in text fixtures as the regression net — every current parser has documented column offsets and tests, so the port is checkable rather than a rewrite on faith. Watch for the off-by-one class of bug the drift doc found: port one module at a time and require `craft-gen build --check` to report `unchanged` after each, which pins the whole document byte-for-byte against the current parsers. Add no further hand-rolled `cifp/` module in the meantime
  - [ ] `when.forcedTransition` (NIITE# GOBBS row) is in the schema and data but no engine module reads it; late-night southbound jets fall through to SSTIK#
  - [ ] `fixtures.test.ts` and the exhaustive test import `@data/ksfo.json` directly; loop over `data/airports.json` before the second airport
- [x] 18. TEC routes, LOA rules, equipment suffixes, destination coordinates, fleet ceilings (`data/ksfo.json` now carries 49 TEC/ADR rows and 3 LOA rules, the LA-basin direct-routing row having been dropped 2026-09-15 as not expressible as a required-token check; the build fails when a TEC row's leading DP is not published for the runway family it departs. `routes.yaml` gained KSAC/KOAK/KSJC for the TEC rows and KVNY/KSNA/KLGB because the ZOA–ZSE LA-basin LOA row names them; none of the six has a `routes` entry yet, so they cannot be drawn as scenarios. The build summary line does not count TEC/LOA rows)
- [ ] 19. Amendment engine — engine landed 2026-09-15 (`web/src/rules/amend/`, fixture runner and `propose` cover the 52 amendment plans); left in this step: the "expect amended" phrasing below — see [amendment-engine.md](./amendment-engine.md) (user decisions 2026-09-15: required nullable `equipmentSuffix` replaces `rnavCapable`; no-SID plans get the assigned SID prepended; proposed altitude is the highest legal one at or below filed; one route rule: assigned SID + filed tail, or the TEC route)
  - [x] (landed `efbc8cd`: `expect.amended`, `resolveAmendedClearance`, row A-EXPECT-AMENDED; the "(minutes) minutes after departure" tail is spoken, confirm with the user) User rule 2026-09-15: when the controller amends the final altitude from what was filed (parity, RVSM band, TEC cap, ceiling), the expect clause is spoken and marked as amended: "expect amended flight level three two zero", "expect amended one zero thousand". Chart note does not cover an amended altitude, so this overrides the `unless_chart_publishes_it` drop. Needs an `expect.amended: true` flag in `ResolvedClearance`/`ExpectedClearance`, a `speak.ts` rendering, and the A-EXPECT rule text extended (confirm with the user whether the "(minutes) minutes after departure" tail is still spoken on the amended form)
- [x] 20. (landed 2026-09-15 through `db7585b`: fault injector, mode switch `m=amend`, amend-then-read session, nine-element results, playtested in Chrome; open questions for the user in the subplan's step D and the NCT route-library finding above) Amendment scenario generator + UI + mode switch — see [amendment-mode.md](./amendment-mode.md) (user decisions 2026-09-15: route box holds the whole route incl. the SID token; RNAV clash accepts either box; up to two injected faults, 20% none; SWA2021 stays unresolved for step 21). **User steer 2026-09-15**: amendment practice must include the occasional already-correct plan, because the skill has two halves, fixing what is wrong and knowing when nothing is; the worksheets do the same. So the fault injector draws "no fault" at a real share (the v1 design's 20%), the UI makes "correct as filed" an explicit answer per box, and grading counts an amendment made to a correct box as a miss. A worksheet amendment fixture may settle with `amendments: []`. **User steer 2026-09-15**: the CRAFT form in unclean mode has the same given rows as clean mode, with C and T read-only, but the procedure half of R is an editable dropdown (the student picks the SID; it is graded there). Open for the step 20 plan: whether the strip's route box amendment then carries the tail only, with the SID chosen in R(1), or the whole route including the SID token as the engine's route rule proposes
- [ ] 21. Validation loop, amendment mode

## Inputs to fold into the rules (user steer 2026-09-15)

- [x] Parse the ZOA CBT module `d14ccce0-8840-11e8-a6af-2a32edb55910` (S1-SFO-0 deck, Google Slides `1yqKhIdUYZlHxmA-mWC_UIX1A1jb7kuMJO3vu_ApTbV8`) into rule rows: done in `generator/airports/ksfo/sop.yaml` and `overrides.yaml` (altitude rows now `climb_via` when a top altitude is published; props → GAPP; SSTIK 1L-only, TRUKN 1R/28s; runway-by-turn preference; staffing fallbacks; no-SID rule)
- [x] Review the S1-SFO-T Major Ground exam results (module `25f8acdc-1afc-11ea-872b-2a32edb55910`): 24/25, the miss was a taxi-route question. Exam questions are not copied into this public repo; the clearance rules they test (28 RT → SSTIK must become WESLA; 28 SO → TRUKN must become SNTNA; SFOE → SSTIK/WESLA become SAHEY, SNTNA/TRUKN become CIITY; heavies may take 28L in 28/01 on request; SAN via the offshore SID) are covered by rule rows and go into synthetic fixtures in step 11

- [x] Operational notice (user, 2026-09-15): "SFO/OAK SEGUL/COAST SID: OFF — issue SSTIK#/WESLA#/CNDEL# YYUNG, CFG SFOW". Modelled as `notices[]` with a `sid_off` effect in `sop.yaml`; the engine skips assignment rows for an off SID so the SSTIK/WESLA YYUNG rows take over. Scenarios can show the notice on the ATIS panel and drill both states.
- [x] User steer 2026-09-15: only TEC routes are obligatory, and only for destinations inside contiguous NCT. Area E CA (the Sacramento sectors: SMF, SAC, MYV, OVE) is contiguous; Area E NV, also called Area R (Reno and satellites), is separated from the rest of NCT by Oakland Center airspace and gets no TEC route. AAR/ADR routes are advisory. Encoded as `tecRoutes[].kind: tec | adr`; amendment mode must flag a missing TEC route only for `kind: tec` rows and only when the destination is `nct: true` (KRNO and the Area R satellites stay `nct: false`)
- [ ] OAK notices for the second-airport backlog: "OAK QUAKE SID: OFF — issue 270 HDG RV first fix for 12/10 jet departures, CFG OAKE"; "OAK SUNNE SID: OFF — issue 120 HDG RV first fix for jet 30 departures, CFG SFOW noise abatement"

- [x] User steer 2026-09-15: write `docs/ADDING_AN_AIRPORT.md`, a runbook of every step taken to build the KSFO data so a dev or agent can repeat it for another airport (update it whenever a loader gains a field or a step changes)

## Blockers

None. Worksheets are public Google Docs (ids in the subplan); no browser needed to fetch them.

## Backlog

- [ ] **v2, user steer 2026-09-15: free-text clearance entry** so students practise without dropdown hints. The student types (or dictates) the full spoken clearance; the grader normalises both sides (digits ↔ number words, "flight level three two zero" ↔ "FL320", punctuation, optional words such as "airport") and aligns the text against the CRAFT elements of `speakClearance` so each element is still graded green/red with its citation, plus a per-element diff showing what was said versus expected. Needs a tolerant matcher (per-element regex or token alignment), a decision on how strict wording is (accept "climb via the SID"? "then as filed" vs "direct"?), and the same seed/URL sharing as v1. Plan as a subplan before starting. The acceptable-but-inefficient verdict (step 14, 2026-09-16) is where two spoken-only readings belong once text is graded: "then as filed" after a bare exit fix, and a full route spelled out where the abbreviated form would do; the user decides whether they are acceptable or wrong
- [ ] **v3, user steer 2026-09-15: KOAK as the second airport** — see [koak-v3.md](./koak-v3.md). Prep done 2026-09-15: OAK ATCT SOP v1.7 downloaded and hashed, 17 DP charts cached, SOP 2-2 tables read, the new rule concepts listed (type-specific class for DH8D, heading departures, hybrid SIDs that are climb-via eligible, continuation charts, approach category, optional noise rows), five OAK worksheets (2 phraseology, 3 amendment) and the S1-OAK-1/2/5 module texts read, the user's "Common Fixes" notes folded in. Next: `generator/airports/koak/worksheets.yaml` + import, CIFP SID inspection, then the rule-concept work
- [x] Callsign telephony table for the spoken reveal (`routes.yaml` `telephony`; schema field `routeLibrary.telephony` added in step 9)
- [ ] Scheduled workflow that re-runs the generator each AIRAC cycle and opens a PR

## Open questions (settled by the validation loops, recorded as data toggles)

See "Open questions" in [craft-trainer-v1.md](./craft-trainer-v1.md#open-questions-to-settle-from-the-worksheets-encoded-as-data-toggles-not-code).
